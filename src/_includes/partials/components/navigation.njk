<nav class="navigation">
    <div class="wrapper">
        <div class="brand">
            <a rel="home" href="/{% if lang != 'en' %}{{ lang }}{% endif %}">
                {% __ "site-title" %}
            </a>
            {#-- <button class="borderless" id="navigation-toggle" aria-expanded="false">{% __ 'menu' %} {% include 'svg/menu.svg' %} {% include 'svg/menu-close.svg' %} </button> --#}
        </div>
        {#-- <div class="menu" id="navigation-menu">
            {% set navItems = collections['pages_' + lang] | eleventyNavigation %}
            <ul>
                {% for item in navItems %}
                <li>
                    <a class="link" href="{{ item.url }}"{% if page.url == item.url %} aria-current="page"{% endif %}>{{ item.key }}</a>
                </li>
                {% endfor %}
            </ul>
        </div> --#}
    </div>
</nav>
